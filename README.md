# sharepoint

Internal tools and automations. Each subfolder is an independent project.

## Projects

| Project | Description | Status |
|---|---|---|
| [cvgen](./cvgen) | CV / Annex E generator with web UI, SQLite backend, Docker deploy | Active |

## Conventions

- One folder per project at repo root
- Each project has its own `README.md` and pipeline
- Docker images published to `ghcr.io/<owner>/<project>:latest`
